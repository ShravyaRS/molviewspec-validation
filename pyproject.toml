[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "molviewspec-validation"
version = "0.1.0"
description = "MolViewSpec-based 3DEM validation visualization (replaces ChimeraX)"
requires-python = ">=3.8"
dependencies = ["molviewspec>=1.0"]
